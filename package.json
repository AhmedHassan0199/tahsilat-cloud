{
  "name": "tahsilat-cloud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply tahsilat-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply tahsilat-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
